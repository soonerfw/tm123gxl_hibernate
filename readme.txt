This has been tested on TM4C 123GXL launchpad,

every 5 seconds, the MCU will wake up, and sample the sensor data and send it through serial port.

The MCU is programmed in hibernate mode (deep sleep).
